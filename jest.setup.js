// import UserModel from './models/userModel.js';

// beforeAll(async () => {

// }, 30000);

// afterEach(() => {
//   const user = UserModel.findOne({ email: 'test@gmail.com' });
//   if (user) UserModel.findByIdAndDelete(user._id);
//   console.log('afterAll called');
// });

// afterAll(() => {
//   console.log('afterAll called');
//   UserModel.deleteOne({ email: 'test@gmail.com' });
// });
